- virsh_memonly_snapshot:
    type = virsh_memonly_snapshot
    start_vm = yes
    variants:
        - scenario:
            variants:
                - xml_diskspec:
                    scenario = xml_diskspec
                - cli_diskspec:
                    scenario = cli_diskspec
