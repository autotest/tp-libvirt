- conf_file.libvirtd_conf.set_audit_logging:
    type = set_audit_logging
    start_vm = no
    variants:
        - positive_test:
            variants:
                - disable_audit_log:
                    enable_audit = no
                - enable_audit_log:
                    enable_audit = yes
