- conf_file.libvirtd_conf.prefix_num:
    type = prefix_num
    start_vm = no
    check_image = no
    take_regular_screendumps = no
    variants:
        - check_prefix_num:
            check_list = "['system.conf', 'default.conf']"
