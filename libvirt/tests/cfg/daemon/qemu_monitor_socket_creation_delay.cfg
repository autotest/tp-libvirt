- daemon.qemu_monitor_socket_creation_delay:
    type = qemu_monitor_socket_creation_delay
    start_vm = no
    check_image = no
    take_regular_screendumps = no
    qemu_wrapper_path = "/tmp/qemu_wrapper.py"
    emulator_dict = {"path": "${qemu_wrapper_path}"}
