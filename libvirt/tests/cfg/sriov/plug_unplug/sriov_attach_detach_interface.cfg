- sriov.plug_unplug.attach_detach_interface:
    type = sriov_attach_detach_interface
    start_vm = "no"
    only x86_64

    variants:
        - with_managed:
            attach_opt = "--managed"
        - without_managed:
            attach_opt = ""
