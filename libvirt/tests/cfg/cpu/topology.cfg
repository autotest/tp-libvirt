- topology:
    type = topology
    only Linux
    start_vm = no
    current_vcpus = 4
    max_vcpus = 4
    cores = 2
    threads = 1
    sockets = 2
    variants:
        - positive:
            variants:
                - hotplug:
                  current_vcpus = 3

