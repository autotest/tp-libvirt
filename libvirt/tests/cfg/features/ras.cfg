- features.ras:
    type = ras
    start_vm = no
    only aarch64
    variants:
        - on:
          ras_state = on
        - off:
          ras_state = off
    variants:
        - positive_test:
            status_error = "no"
