- libvirt_max_vphb:
    type = libvirt_max_vphb
    variants:
        - model:
            only ppc64le
            # ppc supports pc-root architecture model
            model = "pci-root"
            # on guest we can create maximum 32 vphbs
            max_pci = "32"
