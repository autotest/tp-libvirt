- guest_agent.domhostname:
    type = domhostname

    variants:
        - domain_name:
            domain_ref = "name"
        - domain_ID:
            domain_ref = "id"
        - domain_UUID:
            domain_ref = "uuid"
