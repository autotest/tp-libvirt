- virtual_network.qemu_test.boot_with_multiqueue:
    type = boot_with_multiqueue
    only virtio_net
    image_snapshot = yes
    queues_nic1 = 256
    scp_count = 10
    file_transfer_client = "scp"
    filesize = 1024
    vt_ulimit_nofile = 10240
    create_vm_libvirt = yes
    kill_vm_libvirt = yes
    ovmf:
      kill_vm_libvirt_options = --nvram
    Linux:
        msi_check_cmd = "lspci -vvv -s %s | grep MSI"
    Windows:
        no Win2008..sp2
        cdroms += " virtio"
        clean_cmd = del
